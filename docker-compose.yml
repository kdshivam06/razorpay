# PostgreSQL + Redis for RecoveryOS
version: "3.9"

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: recovery
      POSTGRES_PASSWORD: recovery
      POSTGRES_DB: recovery_os
    ports:
      - "5432:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"